# include/

This folder contains local header files not tracked by git.

## secrets.h

Copy the template below and save it as `secrets.h` in this folder:

    #pragma once
    
    #define WIFI_SSID "your_ssid_here"
    #define WIFI_PASSWORD "your_password_here"